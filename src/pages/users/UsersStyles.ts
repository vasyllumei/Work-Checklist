import { styled } from '@mui/material/styles';
export const StyledUsers = {
  Container: styled('div')({
    display: 'flex',
    alignItems: 'start',
    width: '100%',
  }),
  List: styled('div')({
    display: 'flex',
  }),
};
